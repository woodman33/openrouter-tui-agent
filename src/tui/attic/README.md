# attic — quarantined by the p10 feature audit (2026-08-23)

Nothing here is mounted by the live shell. Kept for history; do not import
from live code.

- `router.tsx` — legacy 8-mode router; superseded by the 9-view dispatcher
  shell (src/tui/views.tsx + hooks/useKeyDispatcher.ts).
- `ChatPanel.tsx` — legacy chat panel; superseded by
  components/CommandView.tsx (focus-stack sovereign input).
- `drafts/` — Draft1–5 + TUIDraftShowcase design explorations; never wired
  into navigation; kept as design history.
