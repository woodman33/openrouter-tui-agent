{
  "name": "@timmy/vault-custody",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vault Custody × Timmy — tap-to-verify layer: NTAG 424 DNA SUN verifier, the /t URL scheme, and the custody pages on Astro + Cloudflare Pages.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev dist --port 4399 --kv SESSION --kv CUSTODY_KV",
    "deploy:preview": "wrangler pages deploy dist --project-name vault-custody --branch preview --commit-dirty=true",
    "check": "astro check",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.0",
    "@rive-app/canvas": "^2.42.0",
    "astro": "^5.13.0",
    "qrcode": "^1.5.4",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@cloudflare/workers-types": "^5.20260903.1",
    "@types/qrcode": "^1.5.6",
    "@types/three": "^0.185.4",
    "playwright": "^1.63.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.129.0"
  }
}
