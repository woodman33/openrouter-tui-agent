Inputs below are three delta-schema JSONs (qwen3.8-max = copy rigor, gemini-2.7-flash = pacing/motion, grok-4.6 = punch). Merge per beat: honesty > clarity > punch. Timing must keep total 45.0s. Any claim lacking real evidence is cut. Output ONLY the FINAL beats array JSON: [{"id":"<beat>","claim":"...","sub":"...","evidence":"..."}...] using beat ids: hook1,hook2,turn,lanes,cost,receipt,replay,envlock,clip,local,connect,grammar,split,lockup,end. Include only beats you change; null fields omitted.
